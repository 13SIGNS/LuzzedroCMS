@model IList<LuzzedroCMS.Domain.Entities.Category>
@using LuzzedroCMS.WebUI.Properties

@{
    Layout = null;
}
@foreach (var cat in Model)
{
    string active = Request.Params["category"] == cat.Name ? "active" : string.Empty;
    @Html.ActionLink(@cat.Name, "ArticlesByCategory", new { controller = "Article", action = "ArticlesByCategory", category = @cat.Name })
}